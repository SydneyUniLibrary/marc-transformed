# Record and field values

For many of the methods in the [Record API](#record-api.md), you can give both Record API object as well as Record API
values.

Record API values are simpler to write as object literals, and as so are useful when constructing record or fields by
hand.




## Field values




## Record values
